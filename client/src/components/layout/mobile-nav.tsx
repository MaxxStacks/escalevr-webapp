import { useLocation } from "wouter";
import { useAuth } from "@/hooks/use-auth";

// Composant vide qui ne rend rien puisque nous utilisons maintenant le menu hamburger
export default function MobileNav() {
  return null;
}
